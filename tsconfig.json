{
    "compilerOptions": {
        // z tego folderu bierzemy rzeczy do kompilacji
        "rootDir": "./src",
        // pliki wyjsciowe po przekompilowaniu (JS) beda w folderze dist 
        "outDir": "./dist",
        // wyiowy plik JS bedzie dzialal na starszych przegladarkach przy ES3
        "target": "ES2015"
    }
}